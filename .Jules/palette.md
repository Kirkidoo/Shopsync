## 2024-05-22 - Destructive Action Patterns
**Learning:** Native `confirm()` dialogs are disruptive and inaccessible compared to Shadcn UI `AlertDialog`.
**Action:** Always replace `confirm()` with `AlertDialog` for destructive actions to maintain design consistency and accessibility.
